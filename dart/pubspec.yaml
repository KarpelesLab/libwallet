name: libwallet
description: Pure Dart client for the libwallet cryptocurrency wallet library
version: 0.1.0

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  ffi: ^2.0.0
  hooks: ^1.0.0
  code_assets: ^1.0.0

dev_dependencies:
  test: ^1.24.0
  lints: ^5.0.0
